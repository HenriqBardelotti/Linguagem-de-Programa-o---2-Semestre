#include <stdio.h>
#include <stdlib.h>
#include <locale.h> //biblioteca de idioma

//case sensiteve significa que difrencia maiusculo do minusculo
//sizeof(); tamanho de
// %x vai retornar o valor em exadecimal
// %o vai retornar o valor octagonal
// double = o scanf é referente a ld, exemplo scanf("%ld",x);

int main()
{

    /*setlocale(LC_ALL,"Portuguese");
    //printf("Á atenção");
    int a = 65;
    printf("\n %d %c %x %o",a,a,a,a);

    float N1, N2;
    printf("N1 = ");
    scanf("%f",&N1);
    printf("%.0f\n",N1);
    for (int i = 0; i < N1; i++)
        printf("%.0f + %d = %.0f\n", N1, i, N1+i);*/
    return 0;
}
