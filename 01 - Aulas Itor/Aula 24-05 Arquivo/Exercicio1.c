#include <stdio.h>

int main(){
    FILE *arquivo
    char tarefas[3][100];

    arquivo = fopen("exerc1.text", "w");
    if (arquivo == NULL){
        printf("Error");
        return 1;
    }
    fprintf()
    return 0;
}
