//Escreva um programa em C que solicita ao usuário que insira dois números X e Y, e em seguida exiba todos os números nesta faixa e apresente se é primo ('S') ou não ('N')

#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*int x,y;
    scanf("%d %d",&x, &y);
    for (int i = x; i <=y; i++){
        int primo = 0;
        for (int j = 1; j <= i; j++){
            if (i%j == 0){
                primo++;
            }
        }
        if (primo == 2)
            printf("S");
        else
            printf("N");
    }*/

    return 0;
}
